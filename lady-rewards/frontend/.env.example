# WalletConnect Project ID — enables mobile wallets (MetaMask mobile, SafePal, Trust Wallet, etc.)
# Get a free project ID at https://cloud.walletconnect.com
# Without this, only browser extension wallets (MetaMask desktop, SafePal extension) will work.
VITE_WC_PROJECT_ID=
